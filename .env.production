# Release / Play Store builds (loaded when kReleaseMode or kProfileMode).
# No COC_KEY — the key lives in Cloud Run Secret Manager only.
API_BASE_URL=https://coc-analytics-api-srcfysg3wa-uc.a.run.app/v1
